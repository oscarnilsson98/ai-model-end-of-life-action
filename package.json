{
  "name": "ai-model-end-of-life-action",
  "version": "2.0.0",
  "description": "GitHub Action that flags declared AI models that are deprecated or approaching shutdown.",
  "license": "MIT",
  "private": true,
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oscarnilsson98/ai-model-end-of-life-action.git"
  },
  "scripts": {
    "build": "bun run verify-toolchain && bun build src/main.ts --target=node --format=cjs --outfile=dist/index.js",
    "test": "bun test ./src ./.github/scripts",
    "typecheck": "tsc --noEmit",
    "verify-toolchain": "bun .github/scripts/check-toolchain.ts",
    "check": "bun run verify-toolchain && bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "24.13.3",
    "typescript": "5.9.3"
  },
  "overrides": {
    "@types/node": "24.13.3"
  }
}
