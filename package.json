{
  "name": "ai-model-end-of-life-action",
  "description": "GitHub Action that discovers model usage in committed repository code and warns before known AI model shutdowns.",
  "license": "MIT",
  "private": true,
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oscarnilsson98/ai-model-end-of-life-action.git"
  },
  "scripts": {
    "build": "bun run verify-toolchain && bun build src/main.ts --target=node --format=cjs --outfile=dist/index.js",
    "test": "bun test ./test ./.github/scripts",
    "typecheck": "tsc --noEmit",
    "verify-toolchain": "bun .github/scripts/check-toolchain.ts",
    "check": "bun run verify-toolchain && bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  },
  "overrides": {
    "@types/node": "24.13.3"
  },
  "dependencies": {
    "yaml": "2.9.0"
  }
}
